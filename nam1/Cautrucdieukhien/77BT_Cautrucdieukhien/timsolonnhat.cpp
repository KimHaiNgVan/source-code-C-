#include <iostream>
using namespace std;
int main()
{
	int a, b;
	
	cout << "Nhap vao 2 so a va b:" << endl;
	cin >> a >> b;

	if (a >b ? cout <<"So lon nhat la "<<a : cout<<"So lon nhat la " <<b )
	return 0;
}